[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "critical-minerals-aster"
version = "0.1.0"
description = "ASTER TIR alteration mapping with MRDS validation (multi-site capable)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
  "rasterio>=1.3",
  "geopandas>=0.14",
  "shapely>=2.0",
  "pandas>=2.0",
  "pyyaml>=6.0",
  "scipy>=1.11",
  "statsmodels>=0.14",
  "matplotlib>=3.7",
  "matplotlib-scalebar>=0.8",
  "contextily>=1.4",
  "duckdb>=0.9",
  "pyarrow>=14.0",
  "earthaccess>=0.8",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[project.scripts]
critical-minerals-aster = "critical_minerals_aster.__main__:cli"

[tool.setuptools.packages.find]
where = ["src"]
